# used to calculate the arithmetic mean (average) of numeric data
# calculate all elements in an array or calculate along a specified axis

# example code: 
import numpy as np
array = np.array([1, 2, 3, 4, 5])
mean_value = np.mean(array)
print("Original Array: ", array)
print("Mean Value: ", mean_value)