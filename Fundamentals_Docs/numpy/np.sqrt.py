# used to compute the square root of each element in an array or a single number 

# example code :
import numpy as np
a = np.array([1, 4, 9, 16])
print("Square root of each element in array a: ", np.sqrt(a))

# example with a single number
print("Square root of 25: ", np.sqrt(25))
